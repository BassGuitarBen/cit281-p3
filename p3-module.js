function validDenomination(coin) {
    if (coinage.indexOf(coin) !== -1) {
        return 'true'
    } else {
        return 'false'
    }
}

function valueFromCoinObject(obj){
let obj = {
    denom: 0,
    count: 0,
}
}

function valueFromArray(arr){
const valueFromCoinObject = (obj) => coinage.reduce();
}
function coinCount(...coinage){
console.log(valueFromArray(coinage))
}
let coinage = [
    1, 5, 10, 25, 50, 100
];
const { denom, count} = obj;
module.exports = {
    coinCount,
}